-content_for :submenu do
  =link_to icon('save', 'Save & Exit'), '#', class: 'save-category'
  =link_to icon('trash-o','Delete Category'), admin_category_path(@category),
    data: { method: 'delete',
    confirm: 'Are you sure you want to delete this category?'}

=render 'form'
