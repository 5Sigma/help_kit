= form_for [:admin, @category] do |f|
  .form-group
    =f.label :name
    =f.text_field :name
  .form-group
    =f.label :parent_id, 'Parent category'
    =f.select :parent_id, nested_set_options(HelpKit::Category, @category) {|i| "#{'-' * i.level} #{i.name}" }, prompt: 'No Parent'
    =f.submit
